pipeline{
    agent any
    stages {
        stage('Build'){
            steps{
                bat 'docker run -p 8080:80 --network MyMicroserviceAppNetwork --rm --name recipes recipecatalogue'
            }
        }
    }
}